services:
  - type: web
    name: discord-voice-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: CLIENT_ID
        sync: false
      - key: PORT
        value: 3000
    healthCheckPath: /
    autoDeploy: true